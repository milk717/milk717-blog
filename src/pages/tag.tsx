import React from 'react';
import Layout from '../components/Layout';

const TagPage = () => {
    return (
        <Layout>
            <h1>tag</h1>
        </Layout>
    );
};

export default TagPage;
