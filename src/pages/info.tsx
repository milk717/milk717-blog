import React from 'react';
import Layout from '../components/Layout';

const InfoPage = () => {
    return (
        <Layout>
            <h1>info</h1>
        </Layout>
    );
};

export default InfoPage;
